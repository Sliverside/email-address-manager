@layout({title: 'Edit ' + email + ' (' + email.supplierName + ')'}) 
  <h1>Edit {{ email }} ({{ email.supplierName }})</h1>
  <form method="post" action="{{ route('emails.update', email) }}?_method=PATCH" up-submit up-disable>
    {{ csrfField() }}
    @!input({name: 'description', placeholder: 'description', value: old('description') || email.description })
    @!input({name: 'password', placeholder: 'password' })

    <button type="submit">Update</button>
  </form>
@end